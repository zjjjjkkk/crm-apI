<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.mapper.ContractMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.crm.entity.Contract">
        <id column="id" property="id"/>
        <result column="number" property="number"/>
        <result column="name" property="name"/>
        <result column="amount" property="amount"/>
        <result column="received_amount" property="receivedAmount"/>
        <result column="sign_time" property="signTime"/>
        <result column="customer_id" property="customerId"/>
        <result column="opportunity_id" property="opportunityId"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="delete_flag" property="deleteFlag"/>
        <result column="sign_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="creater_id" property="createrId"/>
        <result column="owner_id" property="ownerId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
    </resultMap>


    <select id="getTradeStatistics" resultType="com.crm.vo.ApprovalTrendVO">
        select any_value(count(*)) as tradeCount,
        any_value(date_format(create_time,'%H:%i')) as tradeTime
        from t_approval
        where delete_flag = 0
        <if test="timeRange[0] !=null and timeRange[1] !=null">
            and create_time between #{timeRange[0]} and #{timeRange[1]}
        </if>
        group by tradeTime;
    </select>


    <select id="getTradeStatisticsByDay" resultType="com.crm.vo.ApprovalTrendVO">
        select any_value(count(*)) as tradeCount,
        any_value(date_format(create_time,${timeFormat})) as tradeTime
        from t_approval
        where delete_flag = 0
        <if test="timeRange[0] !=null and timeRange[1] !=null">
            and create_time between #{timeRange[0]} and #{timeRange[1]}
        </if>
        group by tradeTime;
    </select>

    <select id="getTradeStatisticsByWeek" resultType="com.crm.vo.ApprovalTrendVO">
        select any_value(count(*)) as tradeCount,
        floor((datediff(create_time,date_format(create_time,'%Y-%01-%01')) +
        weekday(date_format(create_time,'%Y-%01-%01'))-1)/7)+1 as tradeTime
        from t_approval
        where delete_flag = 0
        <if test="timeRange[0] !=null and timeRange[1] != null">
            and create_time between #{timeRange[0]} and #{timeRange[1]}
            and create_time >= #{timeRange[0]}
        </if>
        group by tradeTime;
    </select>

</mapper>
